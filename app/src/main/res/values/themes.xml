<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de la aplicación -->
    <style name="Theme.Sensorlab" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colores primarios de la nueva paleta -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- El color de los botones y otros controles -->
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Color de la barra de estado (la barra de arriba del todo) -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <!-- Ocultar el logo de la ActionBar -->
        <item name="logo">@null</item>
        <item name="displayOptions">showTitle</item>
    </style>

    <!-- Tema para la Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Sensorlab</item>
    </style>
</resources>